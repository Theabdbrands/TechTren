files:
  "/etc/nginx/conf.d/static.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
      server {
          listen 80;
          root /var/app/current/dist;
          index index.html;
          location / {
              try_files $uri /index.html;
          }
      }
